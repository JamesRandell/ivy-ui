export default class test {
    
    FirstName: string 
    LastName: string

    constructor(){
      this.FirstName = "";
      this.LastName = "";
    }

    Save(){
      // Send to server....
      alert("Thanks, we have saved this record");
    }
}